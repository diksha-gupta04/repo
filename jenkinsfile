pipeline {
	agent any
	stages {
		stage("Build") {
			steps {
				sh 'mvn clean install'
			}
		}
		stage("Test") {
			steps {
				sh 'mvn compile'
			}
		}
		stage("Run") {
			steps {
				sh 'mvn test'
			}
		}
		
	}
}
